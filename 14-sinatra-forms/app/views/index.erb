<!DOCTYPE html>
<html>
<head>
    <title>Book Index Page</title>
</head>
<body>
    <h1>All the books</h1>

    <ul>
    <% @books.each do |book| %>
        <li><a href="/books/<%= book.id %>"><%= book.title %></a> by <%= book.author %></li>

    <% end %>
    </ul>

</body>
</html>